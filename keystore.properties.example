# Copy to keystore.properties (gitignored) and fill in real values.
# Never commit keystore.properties or the .jks/.keystore file itself.
storeFile=keystore/vexel-release.jks
storePassword=CHANGE_ME
keyAlias=vexel-release
keyPassword=CHANGE_ME
