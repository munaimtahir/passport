# Wave J release verification

Configured release target is `pk.vexel.financepassport`, versionName `1.1.0`, versionCode `4`,
targetSdk `36`, minSdk `26`, with R8 enabled. `bundleRelease assembleRelease` passed. Artifacts:
`app/build/outputs/bundle/release/app-release.aab` and
`app/build/outputs/apk/release/app-release.apk`. The AAB is signed; SHA-256 is recorded by the
local build output verification, and no secret is recorded here.
